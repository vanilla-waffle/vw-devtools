{
  "name": "vw-devtools",
  "version": "1.0.0",
  "description": "Requires an .env file to run",
  "scripts": {
    "start": "docker-compose --env-file .env up -d",
    "mock": "python insert.py"
  },
  "license": "ISC"
}
